/**
 * Eurovision 2026 — Grand Final Running Order
 *
 * Fill this array with country IDs in performance order after it is announced.
 * If empty, GrandFinal.jsx falls back to finalists.js order.
 *
 * Example:
 *   export const grandFinalRunningOrder = [
 *     'se', 'ua', 'fr', 'no', 'it', ...
 *   ]
 */

export const grandFinalRunningOrder = []
